const colors=['pink','yellow','cyan','green'],glyphs=['×','•','✣','╱','◌','●','□','◢','◇'];let mode='all';const grid=document.getElementById('grid'),modes=document.getElementById('modes');[['all','ALL'],['presence','PRESENCE'],['relation','RELATION'],['intention','INTENTION'],['memory','MEMORY']].forEach(([id,label])=>{const b=document.createElement('button');b.className='ctrl '+(id===mode?'active':'');b.textContent=label;b.onclick=()=>{mode=id;document.querySelectorAll('#modes .ctrl').forEach(x=>x.classList.remove('active'));b.classList.add('active');render()};modes.appendChild(b)});function semantic(i){return['presence','relation','intention','memory'][i%4]}function render(){grid.innerHTML='';for(let i=1;i<=365;i++){const sem=semantic(i-1),map={presence:'×',relation:'◌',intention:'╱',memory:'●'},mark=mode==='all'?glyphs[(i-1)%glyphs.length]:(map[mode]||map[sem]),c=document.createElement('button');c.className='cell';c.innerHTML='<span class="n">'+String(i).padStart(3,'0')+'</span><span class="mark '+colors[(i+Math.floor(i/7))%4]+'">'+mark+'</span>';c.title=String(i).padStart(3,'0')+' · '+(mode==='all'?sem:mode);c.onclick=()=>{c.dataset.active='true';c.querySelector('.mark').style.transform='scale(1.7)';setTimeout(()=>c.querySelector('.mark').style.transform='',350)};grid.appendChild(c)}}render();